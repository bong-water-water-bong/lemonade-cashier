"""Sensors: camera, speech, and sensor-fusion *interfaces*.

These modules deliberately do not ship implementations in Phase 1.
They define the event shapes those layers will emit when they exist
so the supervisor can be written and tested today, against recorded
fixtures, without needing actual hardware.
"""
